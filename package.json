{
  "name": "proyek-bot",
  "version": "2.0.0",
  "type": "module",
  "description": "Liviaa🌷 WhatsApp bot using ye-bail (Baileys fork) with pairing endpoint, interactive buttons, list message, admin checks, antispam.",
  "main": "index.js",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=development node --watch index.js"
  },
  "dependencies": {
    "@yemobyte/ye-bail": "^1.0.0",
    "pino": "^9.0.0",
    "fs-extra": "^11.2.0"
  }
}
